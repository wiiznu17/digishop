/*

# Allow all workspace apps and packages
!/apps/
!/apps/**
!/packages/
!/packages/**

# Root configuration files
!package.json
!package-lock.json
!tsconfig.json
!tsconfig.base.json
!.npmrc

# Global ignores (exclude heavy or temporary files)
**/node_modules
.turbo
.vercel
coverage
*.log

# Use Railway's internal build or upload dist if needed. 
# Keeping dist ignored for now as Railway Nixpacks usually builds from source.
**/dist
.next
